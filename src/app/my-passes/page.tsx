import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "My Passes" };

export default function MyPassesPage() {
  return (
    <PagePlaceholder
      title="My Passes"
      description="Your purchased entrance passes and QR codes."
    />
  );
}
