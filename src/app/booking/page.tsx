import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Book a Court" };

export default function BookingPage() {
  return (
    <PagePlaceholder
      title="Book a Court"
      description="Select a date, court, and contiguous time range."
    />
  );
}
