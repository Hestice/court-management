import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "My Bookings" };

export default function MyBookingsPage() {
  return (
    <PagePlaceholder
      title="My Bookings"
      description="Your upcoming and past court reservations."
    />
  );
}
