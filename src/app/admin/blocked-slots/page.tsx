import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Blocked Slots — Admin" };

export default function AdminBlockedSlotsPage() {
  return (
    <PagePlaceholder
      title="Blocked Slots"
      description="Block court/time combinations for maintenance or private events."
    />
  );
}
