import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Users — Admin" };

export default function AdminUsersPage() {
  return (
    <PagePlaceholder
      title="Users"
      description="View all customer accounts and promote users to admin."
    />
  );
}
