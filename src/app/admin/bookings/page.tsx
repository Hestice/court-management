import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Bookings — Admin" };

export default function AdminBookingsPage() {
  return (
    <PagePlaceholder
      title="All Bookings"
      description="Full list with filters for date, court, and status."
    />
  );
}
