import "server-only";

// Stub — contact inquiries are captured via the public contact form but the
// admin list view isn't built yet. When it is, export:
//   listInquiries({ status? })
//   getInquiry(id)
//   countUnresolvedInquiries()
// Follow the pattern in audit-logs.ts.
export {};
